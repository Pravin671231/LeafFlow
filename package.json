{
  "name": "leafflow",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=24.15.0"
  },
  "workspaces": [
    "Backend",
    "admin-app"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "lint": "eslint .",
    "test": "npm run test --workspaces",
    "test:backend": "npm run test --workspace backend",
    "test:admin-app": "npm run test --workspace admin-app",
    "test:coverage:backend": "npm run test:coverage --workspace backend",
    "test:coverage:admin-app": "npm run test:coverage --workspace admin-app"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.9.1",
    "eslint": "^10.4.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.6.0",
    "jiti": "^2.7.0",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.4"
  }
}
